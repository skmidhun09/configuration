eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
spring:
  zipkin:
    enabled: false
  kafka:
    consumer:
       bootstrap-servers: http://localhost:9092
    producer:
       bootstrap-servers: http://localhost:9092